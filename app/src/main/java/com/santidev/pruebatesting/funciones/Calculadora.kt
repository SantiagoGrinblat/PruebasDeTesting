package com.santidev.pruebatesting.funciones

class Calculadora {
  fun sumar(a: Int, b: Int): Int = a + b
  fun restar(c: Int, d: Int): Int = c - d
  fun multiplicar(e: Int, f: Int): Int = e * f
  fun dividir(g: Int, h: Int): Int = g / h
}